# Deliberate exceptions to the slop scan

Each line names the tell number the scanner reports and the client-specific reason it stays.

- [16] em dash: the buy button reads "Demo — checkout not connected", the exact label the handoff mandates for the unconnected checkout; it appears once per product page and nowhere in his copy. His own product prose ("… eventually … Sheaffer Snorkels", "Reach out-we're here") contributes the rest of the dashes the scanner counts on pages that quote him verbatim.
- [12] IntersectionObserver: `static/js/hero.js` uses it only to pause the WebGL loop off-screen (three-hero.md §2 loop discipline), not to fade sections in on scroll; sections have no scroll reveals.
- [10] inline SVG count: the nib-width glyphs on the homepage (five paths that draw the five line widths, the canvas's "Know your nibs" strip) and the search magnifier; no icon set is loaded.
